import 'package:flutter/material.dart';

class ConversionsMainPage extends StatelessWidget {
  const ConversionsMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Conversions Page',
        ),
      ),
    );
  }
}
