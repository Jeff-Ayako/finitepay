import 'package:flutter/material.dart';

class SubAccountsPage extends StatelessWidget {
  const SubAccountsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Subaccounts Page',
        ),
      ),
    );
  }
}
