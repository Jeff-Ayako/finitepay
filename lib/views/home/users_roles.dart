import 'package:flutter/material.dart';

class UsersRoles extends StatelessWidget {
  const UsersRoles({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Users Role Page',
        ),
      ),
    );
  }
}
