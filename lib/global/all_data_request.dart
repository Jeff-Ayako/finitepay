import 'package:flutter/material.dart';

class AllDataRequests extends StatelessWidget {
  const AllDataRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}