import 'package:flutter/material.dart';

class CardMigrationPage extends StatelessWidget {
  const CardMigrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:ListView(children: const [],) ,
    );
  }
}